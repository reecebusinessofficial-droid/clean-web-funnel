# Stripe (server-only — set in .dev.vars for local Cloudflare dev, or worker secrets in production)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PRODUCT_ID=prod_UaeCpKBTq8LYB0

# Optional: canonical site URL for Checkout success/cancel redirects
# SITE_URL=https://your-domain.com

# Mixpanel (client — Vite exposes VITE_* to the browser)
VITE_MIXPANEL_TOKEN=PASTE_TOKEN_HERE
